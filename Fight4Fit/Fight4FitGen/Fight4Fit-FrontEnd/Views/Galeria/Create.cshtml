@model Fight4Fit_FrontEnd.Models.GaleriaModelo

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>GaleriaModelo</legend>
        <div class="editor-label">
            @Html.Label("texto", "Nombre de la Galeria:")
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.NombreGaleria)
         
        </div>
        <div class="editor-label">
            @Html.Label("texto", "Numero evento:")
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Evento)
        </div>

        <p>
            <input type="submit" value="Crear categoria" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
